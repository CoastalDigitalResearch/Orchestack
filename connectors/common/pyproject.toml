[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "orchestack-connector-common"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "nats-py>=2.7",
    "orchestack-envelope",
]
